module.exports = CONFIG = {
    HOST: process.env.HOST || 'http://localhost',
    production: true,
    PORT: process.env.PORT,
    socketIOServer: 'https://accn-presentation-service.herokuapp.com/'
}
